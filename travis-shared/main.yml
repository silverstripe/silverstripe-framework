# Temp: This is just asset-admin/.travis.yml to test cross module imports

language: php

dist: xenial

services:
  - mysql
  - postgresql

env:
  global:
    - SS_ENVIRONMENT_TYPE="dev"

cache:
  directories:
    - $HOME/.composer/cache/files

# Temporary - less matrix entries to speed things up
matrix:
  include:
    # - php: 7.1
    #   env: DB=MYSQL PHPUNIT_TEST=1 COMPOSER_ARG=--prefer-lowest
    - php: 7.2
      env: DB=MYSQL BEHAT_TEST=1
    # - php: 7.3
    #   env: DB=MYSQL PHPUNIT_TEST=1 PHPCS_TEST=1
    - php: 7.4
      env: DB=PGSQL PHPUNIT_TEST=1
    # - php: 7.1
    #   env: DB=MYSQL NPM_TEST=1
    # - php: 7.3
    #   env: DB=MYSQL PHPUNIT_TEST=1 DB=PGSQL
    # - php: nightly
    #   env: DB=MYSQL PHPUNIT_TEST=1 COMPOSER_ARG=--ignore-platform-reqs WITH_XDEBUG=1

before_script:
# Init PHP
  - phpenv rehash
  - if [[ ! $WITH_XDEBUG ]] ; then phpenv config-rm xdebug.ini; fi
  - echo 'memory_limit = 2048M' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - echo 'always_populate_raw_post_data = -1' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini

# Install composer
  - composer validate
  - if [[ $DB == PGSQL ]]; then composer require --no-update silverstripe/postgresql:2.x-dev --prefer-dist; fi
  # TODO: this bit
  - composer require silverstripe/recipe-testing:^1 silverstripe/recipe-cms:4.x-dev silverstripe/frameworktest:0.1.0 --no-update --prefer-dist
  - composer update $COMPOSER_ARG

# Remove preinstalled Chrome (google-chrome)
# this would conflict with our chromium-browser installation
# and its version is incompatible with chromium-chromedriver
  - if [[ $BEHAT_TEST ]]; sudo apt-get remove -y --purge google-chrome-stable || true; fi

# Start behat services
  - if [[ $BEHAT_TEST ]]; then mkdir artifacts; fi
  - if [[ $BEHAT_TEST ]]; then cp composer.lock artifacts/; fi
  - if [[ $BEHAT_TEST ]]; then sh -e /etc/init.d/xvfb start; sleep 3; fi
  - if [[ $BEHAT_TEST ]]; then (chromedriver > artifacts/chromedriver.log 2>&1 &); fi
  - if [[ $BEHAT_TEST ]]; then (vendor/bin/serve --bootstrap-file vendor/silverstripe/cms/tests/behat/serve-bootstrap.php &> artifacts/serve.log &); fi

# Install NPM dependencies
  - if [[ $NPM_TEST ]]; then rm -rf client/dist && nvm install && nvm use && npm install -g yarn && yarn install --network-concurrency 1 && (cd vendor/silverstripe/admin && yarn install --network-concurrency 1) && yarn run build; fi

script:
  - if [[ $PHPUNIT_TEST ]]; then vendor/bin/phpunit tests/php; fi
  - if [[ $NPM_TEST ]]; then git diff-files --quiet -w --relative=client; fi
  - if [[ $NPM_TEST ]]; then git diff --name-status --relative=client; fi
  - if [[ $NPM_TEST ]]; then yarn run test; fi
  - if [[ $NPM_TEST ]]; then yarn run lint; fi
  - if [[ $BEHAT_TEST ]]; then vendor/bin/behat $BEHAT_TEST; fi
  - if [[ $PHPCS_TEST ]]; then composer run-script lint; fi

after_failure:
  - if [[ $BEHAT_TEST ]]; php ./vendor/silverstripe/framework/tests/behat/travis-upload-artifacts.php --if-env BEHAT_TEST,ARTIFACTS_BUCKET,ARTIFACTS_KEY,ARTIFACTS_SECRET --target-path $TRAVIS_REPO_SLUG/$TRAVIS_BUILD_ID/$TRAVIS_JOB_ID --artifacts-base-url https://s3.amazonaws.com/$ARTIFACTS_BUCKET/ --artifacts-path ./artifacts/; fi

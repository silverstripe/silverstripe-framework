/* global jest, describe, beforeEach */

jest.unmock('lib/SilverStripeComponent');
jest.unmock('../FormBuilder');

// import { FormBuilder } from '../';

describe('FormBuilder', () => {
  describe('getFormSchema()', () => {
    /*
    let formBuilder;

    beforeEach(() => {
      const props = {
        store: {
          getState: () => {},
        },
        actions: {},
        schemaUrl: 'admin/assets/schema/1',
        schema: { forms: [{ schema: { id: '1', schema_url: 'admin/assets/schema/1' } }] },
      };

      formBuilder = new FormBuilder(props);
    });
    */
  });
});
